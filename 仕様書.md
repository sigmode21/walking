# 人流検討ツール 仕様書

最終更新: 2026-07-19 / 対象ファイル: `index.html` (単一HTMLファイル)

---

## 1. 概要

### 1.1 目的

空港・駅・プール・温浴施設・学校・競馬場のバス乗り場など、**複数の出入口から多数の人が流入し、容量に限界のあるボトルネック**(改札・ゲート・階段・更衣室・乗車口など)で滞留が発生する施設を対象に、混雑・行列の発生箇所を可視化し、設計検討・改善検討を支援するツール。

### 1.2 位置づけ

- **社内での設計検討・確認用**。避難安全検証などの**法的用途には使用できない**(画面ヘッダーに常時明示)。
- 計算ロジックは決定論的に実装。AIは「結果の解釈」「改善案の言語化」に限定し、計算には一切関与しない(AI連携はオプション機能)。
- 汎用エンジン方式: 施設タイプごとに個別ツールを作るのではなく、同一エンジンに対して「ノード構成+パラメータ」の切り替え(プリセット/シナリオ)で複数施設に対応する。

### 1.3 動作環境

- 単一HTMLファイル。外部ライブラリ・ネット接続・サーバー不要。ブラウザで開くだけで動作する。
- 図面単位は mm 固定 (DXF準拠、1000mm = 1m)。

---

## 2. 計算モデル

### 2.1 基本方式

**ネットワーク型・決定論的流体近似モデル**。空間をノード(流入・分岐・滞留・流出)とリンク(通路)のグラフとして表現し、**dt = 1秒**の時間ステップで流量保存を解く。乱数は一切使用せず、同じ入力は必ず同じ結果になる。

人は流体(連続量)として扱う。個人単位の経路・属性の追跡は行わない(→ §6 制約)。

### 2.2 ノード種別

| 種別 | 表示 | 役割 | 主なパラメータ |
|---|---|---|---|
| 流入 (source) | 緑丸 | 人の発生源 | 定常流入 [人/分] / 定常流入の時間変化 (任意) / 一斉到着の間隔 [分]・人数 [人]・放出時間 [分] |
| 分岐 (junction) | 青丸 | 通路の合流・分岐点 | 滞留時間 [分] (0=即通過) |
| 滞留部 (gate) | 黄枠 | 容量制約のあるボトルネック | 通路数 × 処理能力 [人/分/通路] / 形状(円・矩形・多角形) / 同一設備の設定 |
| 流出 (sink) | 灰丸 | 人の消滅先 | 定常流出 [人/分] / 定常流出の時間変化 (任意) / 一斉出発の間隔 [分]・人数 [人]・放出時間 [分] (定常・一斉とも0なら無制約) |

**一斉到着・一斉出発**は、指定間隔ごとに指定人数を**「放出時間」(既定0.5分) にわたり均等に処理**するパルスとして扱う。放出時間は現象に合わせて設定する — 流入側: 列車降車0.5分 / 送迎バス降車1分 / 教室退出1〜3分 / スタンド退場5〜15分など。流出側: 列車乗車0.5分 / バス乗車3〜4分 (停車中に乗り込む時間) など。流出が追いつかない分は流出ノードに行列として滞留する。

**定常流入・定常流出の時間変化** (任意): 「時刻[分]:人数/分」のカンマ区切り (例 `0:2, 60:5, 120:1`) を指定すると、定常流入 (または流出ノードの定常流出) がその時刻から次の指定まで**階段状に変化**する。流入側は夕方ピーク・開店直後の山・ラッシュの立ち上がりなど、流出側は出口の時間帯開放・タクシー乗り場の供給変動などを表す。指定時は一定値の欄は無視される。パルス (一斉到着・出発) との併用可。

#### 分岐の滞留時間 (stayMin)

浴場・飲食・観覧など「**しばらく滞在してから次へ進む**」行動を表現する。到着者を指定時間の遅延線で保持したのち下流へ送出する。到着波形は保持され、そのままの形で退出する(30秒のパルスで入れば30秒のパルスで出る)。収容人数は無制限(必要なら上流の設備定員で在館数を制限する)。下流が満杯の間は「退出待ち」として保持される。

### 2.3 リンク (接続)

| パラメータ | 意味 |
|---|---|
| 分配比 (重み) | 同一ノードから複数リンクへ送出する際の比率 |
| 通路幅 [m] | ヒートマップの密度算出、およびスピルバック時の収容量に使用 |

- 歩行時間 = リンク長 × 図面スケール ÷ 歩行速度 (折れ点を含む経路長で算出)。
- リンク収容量 = 長さ × 幅 × 滞留密度 2.5人/m² (スピルバック時の上限)。

**混雑回避分配**: 複数の下流へ分岐するノード (流入・分岐・滞留部) では、固定の分配比の代わりに「混雑回避で分配」を選べる。下流の混雑水位 (リンク上の人数+行列を分配比で正規化した値) が均されるよう毎ステップ決定論的に配分し、「行列の短い方へ人が流れる」「誘導員が空いた方へ振る」挙動を表す。分配比は能力バランスの正規化 (広い通路ほど多く受け持つ等) に使われる。

### 2.4 滞留部 (ボトルネック) の処理

毎ステップで次を行う。

1. **受け入れ**: 滞留エリアの空き(面積 × 2.5人/m² − 現在の在圏)の範囲で上流リンク末端から受け入れる。
2. **処理・送出**: 処理能力 (通路数 × 人/分/通路) の範囲で下流リンクへ送出する。下流が詰まっていれば送れない。
3. **行列人数** = エリア内の人数 + 上流リンク末端で詰まっている人数。

**待ち時間**は累積到着曲線と累積退出曲線の水平距離 (FIFO仮定) として算出する。シミュレーション時間内に捌けない場合は「≥残り時間」と表示する。

### 2.5 同一設備 (入側・出側の共用)

更衣室や双方向改札のように、**入る人と出る人が同じ一つの室・設備を使う**状況を表現する仕組み。滞留部ノードに同じ「設備名」を設定すると、それらは一つの設備の入側 (`役割=入側`)・出側 (`役割=出側`) として扱われ、次の3つを共有する。

| 共有項目 | 内容 |
|---|---|
| **滞留面積** | 常に有効。設備内ノードの面積を合算し、在圏人数も合算して満杯判定する (スピルバック時)。 |
| **設備の定員** [人] | ロッカー・脱衣スペース等。**入側への入室時に占有され、出側からの退室完了時に解放**される。満杯の間は入側の入室が待たされる (温浴施設の入場制限に相当)。0 = 無制限。 |
| **設備の処理能力** [人/分] | 双方向の改札・階段、入退館を1箇所で担うフロント等。設備全体の通過能力を、入側・出側の**行列人数に応じて毎ステップ分け合う** (全て空なら等分)。各ノード自身の処理能力も同時に上限として働く (方向で速さが違う場合は各ノードの能力に個別値を設定)。0 = 各ノード個別。 |

定員・処理能力は設備内で共通 (複数ノードに異なる値を入れた場合は最大値を採用)。占有状況は図面上(入側ノードに「占有 63/80人」)、結果サマリー、AI用データに出力される。

### 2.6 スピルバック (オプション)

オンにすると、滞留エリア (面積 × 2.5人/m²) とリンク (長さ × 幅 × 2.5人/m²) の収容量を上限とし、**満杯時は詰まりが上流へ波及**する (流入ノードまで詰まれば発生源に滞留=ホーム待機等)。図面スケールとノード面積を実寸に合わせてから使用すること。オフの場合、収容量は表示上の目安で、行列人数に上限はない。

### 2.7 主要な既定値・基準値

| 項目 | 値 |
|---|---|
| 時間ステップ | 1秒 |
| 歩行速度 | 既定 1.2 m/s (0.3〜2.0で変更可)。利用者層の目安: 通勤者1.3 / 一般1.2 / 高密度の群集1.0 / 温浴館内1.0 / 児童1.1 / トレイ搬送1.1 |
| 滞留密度 (行列・収容量算定) | 2.5 人/m² |
| 一斉到着・出発の放出時間 | 各ノードで可変 (既定0.5分) |
| シミュレーション時間 | 1〜180分。目安: 現象が終わる長さ+余裕 (周期現象は数周期分) |

---

## 3. 画面と操作

### 3.1 画面構成

- **左ツールバー**: モード切替 (選択・移動 / 流入・分岐・滞留・流出の追加 / 接続)。
- **中央キャンバス**: 図面。ホイールでズーム、背景ドラッグでパン。
- **右パネル**: シナリオ管理、シミュレーション設定、下絵、ヒートマップ、プロパティ、結果サマリー、操作説明。
- **下部タイムライン**: 再生ボタン (×0.25〜×4)、経過時刻スライダー、**イベントマーカーバー** (スライダー直上。緑=一斉到着 / 青=一斉出発 / 赤帯=設備の定員満杯の期間。クリックでその時刻へジャンプ)、時系列グラフ4枚:
  1. **行列人数** [人] (ボトルネック別)
  2. **待ち時間** [分] (ボトルネック別)
  3. **累積流入・流出図** — 選択中のボトルネック (未選択時は最大行列のノード) の累積到着・累積退出の2曲線。**横の開き=待ち時間、縦の開き=行列人数、間の面積=延べ待ち時間**として読める待ち行列解析の標準図法
  4. **設備の占有・在圏** [人] — 設備の定員 (ロッカー・座席等) の占有数と定員線 (破線)、滞留ノード (浴場・食事ホール等) の在圏人数の時系列

### 3.2 シナリオ管理

- **施設プリセット**: 「駅改札」「イベント退場・バス乗場」「温浴施設・更衣室」「学校の階段 (授業終了)」「学生食堂 (昼休み)」の5種。ドロップダウン変更で即読込 (詳細は別紙『シナリオ解説』)。プリセットはノード構成に加え、**シミュレーション時間・歩行速度・スピルバック設定も施設に応じた値に設定**する (駅60分/1.3m/s、イベント90分/1.0m/s、温浴180分/1.0m/s、学校15分/1.1m/s、食堂60分/1.1m/s)。
- **シナリオの説明ガイド**: プリセット読込時、ドロップダウン直下に「このシナリオの説明」(想定・動線・主な設定・条件・見どころ) が開閉式で表示される。
- プリセットのドロップダウンは、**新規作成・シナリオ読込を行うと「— 選択してください —」表示に戻る** (画面の内容がプリセットとは別物になったことを示す)。
- **シナリオ保存/読込**: ノード・接続・下記パラメータをJSONファイルに保存/読込 (DXF下絵は含まない)。読込時は再生位置をリセットし、シナリオ名をヘッダーに表示。
- **新規作成**: ノード・接続・下絵・計算結果をすべて消去し、設定を初期値 (60分・1.2m/s・自動再計算ON・スピルバックOFF等) に戻す。
- **自動再計算**: オンの間、パラメータ変更のたびに即時再計算・表示更新される。

### 3.3 下絵 (DXF / 画像)

- DXFの LINE / LWPOLYLINE / CIRCLE / ARC を下絵として表示 (壁・通路の自動認識はしない)。PNG / JPG も可。
- DXFの作図単位 (mm / m) を指定可能。
- **下絵の編集モード**: ドラッグで移動、四隅ドラッグで拡大縮小 (アスペクト比維持)、Shift+ドラッグで回転。数値入力 (X/Y移動・拡大率・回転角) でも調整可。編集中もホイール=画面ズーム、中ボタンドラッグ=画面パン。
- プリセット変更・シナリオ読込時に下絵はクリアされる。

### 3.4 ヒートマップ

- 各リンクの通路幅から**密度 [人/m²]** を算出し、約0.8mグリッドで図面上に着色 (青→緑→黄→赤)。
- **表示モード**: 「現時刻の密度」(スライダー・再生に連動) / 「全時間の最大密度」(全時間の最悪状態を1枚で示す静的マップ。報告資料向き)。
- 行列は**滞留密度2.5人/m²の帯**としてボトルネック上流側へ物理的な長さで伸ばして描画 (「行列がどこまで届くか」が図面上で読める)。
- スケール上限 [人/m²] と不透明度を調整可能。凡例カラーバー付き。
- **歩行者ドット表示** (既定オフ): 人を点として描画する表示専用の演出 (計算には影響しない。配置揺らぎも乱数ではなく決定論的ハッシュで生成)。**歩行中の人**はリンク上をまばらに流れる点、**行列**は滞留帯の中に密に並ぶ点として描き分けられ、点の色は局所密度に応じてヒートマップと同系 (白→黄→橙→赤) に変化する。1点=1人が基本で、極端な大群衆時のみ描画上限内に集約する。

### 3.5 図面上の情報表示

- リンク: 流動量による色分け (青→赤)、長さ・幅・収容定員・重み (分岐時は%併記)。
- 滞留部: 行列人数の赤バー (平方根スケール)、待ち時間、面積・収容人数 (スピルバック時)、設備の占有状況 (入側に「占有 n/N人」)。
- 分岐 (滞留時間あり): 「滞在n分 / 在圏n人」。
- 流入・流出: 流入/流出条件、一斉到着・出発の発生中は広がるリングで明示。流出は累計通過人数。

### 3.6 キー・マウス操作

| 操作 | 機能 |
|---|---|
| ホイール / 背景ドラッグ | ズーム / パン |
| 接続モード: ノード→空白クリック→ノード | 折れ線リンクの作成 |
| リンク選択中: ダブルクリック / ドラッグ / 右クリック | 折れ点の追加 / 移動 / 削除 |
| Del / Ctrl+Z / Shift / Esc | 削除 / 元に戻す / 水平・垂直ロック / キャンセル |

滞留部の形状は円形 (直径)・矩形 (幅×高さ)・多角形 (クリックで頂点追加、スナップで閉合確定、頂点ドラッグ編集) をハンドルまたは数値で編集できる。

---

## 4. 出力

| 出力 | 内容 |
|---|---|
| **結果サマリー** | ボトルネック別の最大行列・発生時刻・最大待ち・行列発生時間・処理能力、設備の定員の最大占有・満杯時間、総通過人数、ルールベースの所見 (最悪ボトルネックの特定と通路増設等の示唆) |
| **CSV出力** | ボトルネック別の行列人数・待ち時間の時系列を10秒間隔で出力 (Excel対応・BOM付きUTF-8、`walking-sim-result.csv`) |
| **AI用データ** | 結果JSON (ノード構成・ボトルネック統計・設備定員統計・総通過人数) をクリップボードへコピー。Claude等に貼り付けて解釈・改善提案の言語化を依頼する方式。計算には関与しない |
| **レポート出力 (PNG)** | ヘッダー (シナリオ名+免責)・図面の現在表示・グラフ4枚・結果サマリー全文を**1枚のPNG画像に合成**してダウンロード (`シナリオ名-report.png`)。「全時間の最大密度」モードに切り替えてから出力すると報告資料向きの1枚になる |

### シナリオファイル形式 (JSON)

```json
{
  "app": "walking-sim-prototype",
  "version": 1,
  "name": "シナリオ名",
  "params": { "durMin": 120, "speed": 1.2, "spill": true, "heatMax": 3 },
  "nodes": [ { "id": "n1", "type": "source|junction|gate|sink", "x": 0, "y": 0, "label": "...",
               "rate": 0, "trainInterval": 0, "trainPax": 0,
               "capPerLane": 0, "lanes": 0, "shape": "circle|rect|poly",
               "stayMin": 0, "share": "設備名", "shareRole": "in|out",
               "poolSize": 0, "shareCap": 0, "...": "形状寸法ほか" } ],
  "links": [ { "id": "l1", "from": "n1", "to": "n2", "weight": 1, "width": 3, "pts": [] } ]
}
```

---

## 5. 参考: 容量の目安

プリセットで採用している換算値の例。

| 要素 | 目安 |
|---|---|
| 自動改札 (IC主体) | 40〜60 人/分/通路 |
| 階段 (下り) | 約65 人/分/**有効幅1m** (実幅1.4m→有効1.2m→約78人/分。手すり・側壁分0.2mを控除) |
| 階段 (上り) | 約56 人/分/**有効幅1m** (下りより遅い)。上り下りが同じ階段を使う場合は同一設備の共用処理能力で表現 |
| 出口・扉 | 約80 人/分/幅1m |
| 更衣室 | 同時更衣人数 ÷ 平均更衣時間 (入りより湯上がりが遅い) |
| バス乗車 (前扉ICタッチ) | 約20 人/分/扉 |
| 昇降口 (靴の履き替え) | 約75 人/分/列 |
| エスカレーター (1000型・上り) | 実効 約60 人/分/基 |
| 券売機 (現金・選択操作) | 約5 人/分/台 |
| 配膳・食器返却 | 約10 人/分/列 |
| 食堂の座席 | 席数 ÷ 平均滞在時間 (350席・20分 → 17.5人/分) |

多層階の階段は**階の区間ごとに滞留部として分割し、各階の踊り場 (分岐ノード) で下階の流入と合流させる**ことで、下階が階段を埋めて上階が降りられなくなる現象まで表現できる (プリセット「学校の階段」参照)。

---

## 6. 制約・注意事項

1. **法的用途不可**: 避難安全検証等には認められた手法・ツールが必要。本ツールは社内検討用。
2. **流体近似**: 人を連続量として扱うため、個人の経路・属性は追跡しない。分岐後の行き先は分配比 (重み) による比例配分であり、「この人がどこへ行くか」は表現しない。
3. **滞留時間ノードの収容は無制限**: 浴場等の定員そのものは持たない (上流の設備定員で在館数を制限する使い方を想定)。
4. **モデルの妥当性**: 容量・密度などの基準値は公知の目安を採用しているが、計算結果を設計判断に用いる際は前提条件 (図面スケール・面積・容量値) の確認を行うこと。
5. **DXFは下絵**: 図面からの通路自動認識はしない。ノード・リンクは手動配置。
